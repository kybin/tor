if -> switch for some places.
some do event parsing now exist outside of do() func.
find mode place?
replace mode?
correct comment parser per languages. (at least go and python)
shoud a cursor edit it's text directly?
window resize handling does not working on maximize event.
draw one more cell at the end of lines for indicate selection.
Logger?
Offset changers for other than cursor line.

